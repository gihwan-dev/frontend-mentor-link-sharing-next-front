import styles from "./addlink.title.module.scss";

const AddLinkTitle = () => {
  return (
    <div className={styles.title}>
      <h1>Customize your links</h1>
      <p>
        Add/edit/remove link below and then share all your profiles with the
        world!
      </p>
    </div>
  );
};

export default AddLinkTitle;
